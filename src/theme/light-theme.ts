import { Theme } from './types';
import { palette } from './color-palette';

export const lightTheme: Theme = {
  primaryBackground: palette.white,
  primaryHeading: palette.onyx,
  variant: 'light',
};
