import { Theme } from './types';
import { palette } from './color-palette';

export const darkTheme: Theme = {
  primaryBackground: palette.onyx,
  primaryHeading: palette.white,
  variant: 'dark',
};
